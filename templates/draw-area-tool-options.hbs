<div id="linemap-tool-options">
    <div class="linemap-tool-shelf">
        {{#each styles as |style styleName|}}
            <a class="linemap-style-select area {{#if (eq styleName ../activePattern)}}active{{/if}}" data-action="set-area-pattern" data-area-pattern="{{styleName}}"><img src="{{style.texture}}" /></a>
        {{/each}}
    </div>
</div>
